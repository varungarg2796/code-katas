// https://www.codewars.com/kata/55685cd7ad70877c23000102


// Make negative, if already negative, then do nothing.
function makeNegative(num) {
    // Code?
    if ( num < 0 ) {
      return num;
    } else { 
      return -num;
    }
  }

// OR 

//   function makeNegative(num) {
//     return num < 0 ? num : -num;
//   }